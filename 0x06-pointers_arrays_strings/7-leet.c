#include "main.h"
#include <string.h>

/**
 * leet -  function that copies a string.
 * @a: array
 * Return: the pointer is the dest string
 */
char *leet(char *a)
{
	int k;
}
	for (k = 0; k < n; k++)
	{
		if (a[k] == 'a' || a[k] == 'A')
			a[k] = '4';
		else if (a[k] == 'e' || a[k] == 'E')
			a[k] = '3';
		else if (a[k] == 'o' || a[k] == 'O')
			a[k] = '0';
		else if (a[k] == 't' || a[k] == 'T')
			a[k] = '7;
		else if (a[k] == 'l' || a[k] == 'L')
			a[k] = '1';
		else
	}
	return (a);
}
