#include "main.h"
#include <ctype.h>
/**
 * add - prints 10 times the alphabet, in lowercase,
 * followed by a new line.
 * @a: number 1
 * @b: number 2
 * Return: 0
 */

int mul(int a, int b)
{
	int mult = a * b;

	return (mult);
}
