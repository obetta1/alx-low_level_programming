#include "main.h"
/**
 * add - prints 10 times the alphabet, in lowercase,
 * followed by a new line.
 * @a: number 1
 * @b: number 2
 * Return: 0
 */

void print_numbers(void)
{
	int i;

	for (i = 48; i < 59; i++)
	{
		_putchar(i);
	}
	_putchar('\n');

	return (mult);
}
