#include "main.h"
#include <string.h>
/**
 * _strlen - returns the lenght on string
 * @s: string
 *
 * Return: the lenght of the strung
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
