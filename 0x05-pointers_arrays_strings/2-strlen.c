#include "main.h"
#include <string.h>
/**
 * _strlrn - returns the lenght on string
 * @s: string
 *
 * Returns: the lenght of the strung
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
