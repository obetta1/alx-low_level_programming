#include "main.h"
#include <ctype.h>
/**
 * _abs - prints the absolutee value of a  given number,
 * followed by a new line.
 * @c: The number
 *
 * Return: 0
 */

int _islower(int c)
{
	int n = abs(c);

	printf("%d", n);
	return (0);
}
